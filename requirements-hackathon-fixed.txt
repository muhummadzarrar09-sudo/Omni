# OMNI V3 FIXED - PINNED BUT WORKING ON PYTHON 3.12 WIN - 2026-07-13
# If torch fails, run second line without torch first
# Install: pip install -r requirements-hackathon-fixed.txt

# === CORE TORCH PINNED FOR 1050 Ti - Install via cu121 index ===
# First try: pip install torch==2.2.2 torchaudio==2.2.2 --index-url https://download.pytorch.org/whl/cu121
# Then install rest
torch==2.2.2
torchaudio==2.2.2
numpy==1.26.4

# === FIXED: psutil version that EXISTS on PyPI for cp312 win ===
psutil>=5.9.0
psutil==6.1.1
packaging>=24.0

# === AUDIO - SINGLE ENGINE FIX ===
# PyAudio fails with -9999, so we use sounddevice as primary, pyaudio fallback
sounddevice==0.4.6
PyAudio==0.2.13
faster-whisper==1.0.3

# === TTS SINGLE ===
pyttsx3==2.90
# kokoro-onnx optional - needs model files, not required for SAPI5 fallback
# kokoro-onnx==0.4.0
# espeakng-loader==0.2.4

# === UI SINGLE ===
PyQt5==5.15.11
PyQt5-sip==12.15.0
PyQt5-Qt5==5.15.2
PyQtWebEngine==5.15.6

# === TOOLS ===
mss==9.0.0
Pillow==10.4.0
pyautogui==0.9.54
pyperclip==1.8.2
requests==2.32.0
python-dotenv==1.0.0
pydantic==2.0.0

# === LOGGING ===
loguru==0.7.2
colorama==0.4.6
colorlog==6.9.0
