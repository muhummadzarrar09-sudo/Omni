# Compatibility entry point. Profile definitions in ../pyproject.toml are the
# single source of truth. Run this from the repository root for a non-editable
# core-profile source build. Prefer the exact platform lock documented in
# docs/TROUBLESHOOTING.md for a reproducible qualified environment.
.[core]
