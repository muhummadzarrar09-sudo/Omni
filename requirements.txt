# OMNI V2 - Requirements - JARVIS KILLER - Phase 1 Complete
# Python 3.10+ | GTX 1050 Ti 4GB Optimized | Fixed for Python 3.12 + numpy 2.x
# Phase 1: Clean + Multi-Agent Skeleton + 100+ Tools Routing + Chain Commands

# --- CORE (Must have, makes CLI work) ---
loguru>=0.7.0
numpy>=1.26.0
psutil>=5.9.0
packaging>=24.0

# --- UI - Cinematic HUD ---
PyQt5>=5.15.10
PyQt5-sip>=12.15
PyQt5-Qt5>=5.15.2
PyQtWebEngine>=5.15.6  # For Three.js orb

# --- VOICE - STT + VAD + Wake Word ---
faster-whisper>=1.0.3
sentence-transformers>=2.2.2
torch>=2.0.0
torchaudio>=2.0.0 ; platform_system == "Windows"
onnxruntime>=1.18.0
PyAudio>=0.2.13
sounddevice>=0.4.6
openwakeword>=0.6.0  # Wake word "Hey OMNI" offline
pvporcupine>=3.0.0   # Alternative wake word

# --- TTS - Local First Cinematic ---
kokoro-onnx>=0.4.0
pyttsx3>=2.90
espeakng-loader>=0.2.4 ; platform_system == "Windows"

# --- MULTI-AGENT + MEMORY (V2 New) ---
chromadb>=0.4.0  # Vector store for persistent memory
ollama>=0.3.0  # Local LLM llama3.1
langchain>=0.2.0  # Agent framework
langchain-community>=0.2.0
mem0ai>=0.1.0  # Memory extraction

# --- VISION - Screen + Camera ---
mss>=9.0.0  # Fast screen capture
opencv-python>=4.8.0
Pillow>=10.0.0
pyscreeze>=0.1.30
easyocr>=1.7.0  # OCR for read_text_on_screen

# --- SECURITY - Biometric ---
# face_recognition needs dlib - optional for Phase 2
# face-recognition>=1.3.0  # Uncomment when dlib installed
# deepface>=0.0.90  # Alternative

# --- AUTOMATION - 100+ Tools ---
pyautogui>=0.9.54
pyperclip>=1.8.2
comtypes>=1.1.14 ; platform_system == "Windows"
keyboard>=0.13.5
websocket-client>=1.6.0
websockets>=12.0

# --- SYSTEM + LOGGING ---
colorlog>=6.9.0
coloredlogs>=15.0
tqdm>=4.60.0
requests>=2.31.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# --- Original V1 Deps Kept ---
faster-whisper>=1.0.3
sentence-transformers>=2.2.2
# simpleaudio optional for <3.12
simpleaudio>=0.2.14 ; python_version < "3.12"
