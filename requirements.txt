# OMNI V2 - Requirements - Phase 3.5 Turbo - HF + llama.cpp + TurboVLM WAY FASTER than Ollama
# Python 3.10+ | GTX 1050 Ti 4GB Optimized | Fixed for Python 3.12 + numpy 2.x

# CORE
loguru>=0.7.0
numpy>=1.26.0
psutil>=5.9.0
packaging>=24.0

# UI - Cinematic HUD
PyQt5>=5.15.10
PyQt5-sip>=12.15
PyQt5-Qt5>=5.15.2
PyQtWebEngine>=5.15.6

# VOICE - STT + VAD + Wake Word - Phase 4 Accessibility 4 Tiers
faster-whisper>=1.0.3
sentence-transformers>=2.2.2
torch>=2.0.0
torchaudio>=2.0.0 ; platform_system == "Windows"
onnxruntime>=1.18.0
PyAudio>=0.2.13
sounddevice>=0.4.6
openwakeword>=0.6.0
pvporcupine>=3.0.0
# NEW - 4-Tier STT for Accessibility - Actually HEARS Everyone
RealtimeSTT>=0.3.0  # Tier 1 - Most robust, streaming, Silero VAD + Whisper
vosk>=0.3.45  # Tier 2 - Offline 50MB, lightweight, no internet
SpeechRecognition>=3.10.0  # Tier 3 - Google cloud fallback, super reliable
pyaudio>=0.2.13  # Already above, but needed for all STT

# TTS
kokoro-onnx>=0.4.0
pyttsx3>=2.90
espeakng-loader>=0.2.4 ; platform_system == "Windows"

# LLM - Phase 3.5 Turbo - HF + llama.cpp WAY FASTER than Ollama
huggingface_hub>=0.20.0
# llama-cpp-python needs special install for CUDA: pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121
llama-cpp-python>=0.2.80
chromadb>=0.4.0
ollama>=0.3.0
langchain>=0.2.0
langchain-community>=0.2.0
mem0ai>=0.1.0

# VISION - Phase 3.5 TurboVLM EVEN FASTER than LLaVA
mss>=9.0.0
opencv-python>=4.8.0
Pillow>=10.0.0
pyscreeze>=0.1.30
easyocr>=1.7.0
# TurboVLM:
moondream>=0.1.0
# qwen-vl-utils for Qwen2-VL
qwen-vl-utils>=0.0.8
transformers>=4.40.0

# SECURITY
# face_recognition needs dlib - optional
# face-recognition>=1.3.0

# AUTOMATION 100+ Tools
pyautogui>=0.9.54
pyperclip>=1.8.2
comtypes>=1.1.14 ; platform_system == "Windows"
keyboard>=0.13.5
websocket-client>=1.6.0
websockets>=12.0

# SYSTEM
colorlog>=6.9.0
coloredlogs>=15.0
tqdm>=4.60.0
requests>=2.31.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# TTS Fallbacks for Bagillion Loop - Never fails to speak
gTTS>=2.4.0
playsound>=1.3.0
pydub>=0.25.0
