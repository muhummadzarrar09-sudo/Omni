# OMNI - Requirements (Winning Edition) - FIXED for Python 3.12
# Python 3.10+ | Windows 10/11 optimized for GTX 1050 Ti, but cross-platform friendly
# FIX: numpy<2.0 caused conflict with kokoro-onnx>=0.4.0 which needs numpy>=2.0.2
# Now allows numpy 2.x and uses onnxruntime>=1.18 for numpy2 support

# IMPORTANT Windows first-time setup (Admin PowerShell):
#   Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
#   pip install --upgrade pip wheel setuptools

# --- CORE (MUST HAVE - install these first if full install fails) ---
# These 2 are enough for CLI mode: python omni.py --test
loguru>=0.7.0
numpy>=1.26.0
psutil>=5.9.0
packaging>=24.0

# --- UI ---
PyQt5>=5.15.10
PyQt5-sip>=12.15
PyQt5-Qt5>=5.15.2

# --- Voice - Speech to Text ---
# faster-whisper will pull ctranslate2, huggingface-hub, tokenizers, av, tqdm
faster-whisper>=1.0.3
sentence-transformers>=2.2.2

# Torch stack - CPU by default, CUDA auto-detected if installed
# NOTE: For GTX 1050 Ti CUDA, you may later run:
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchaudio>=2.0.0 ; platform_system == "Windows"

# ONNX Runtime - MUST be >=1.18.0 for numpy 2.x support
onnxruntime>=1.18.0

# --- Audio ---
PyAudio>=0.2.13
sounddevice>=0.4.6
# simpleaudio often fails on Python 3.12, optional
simpleaudio>=0.2.14 ; python_version < "3.12"

# --- TTS - LOCAL FIRST ---
# kokoro-onnx 0.4.0+ needs numpy>=2.0.2, now allowed
kokoro-onnx>=0.4.0
pyttsx3>=2.90
espeakng-loader>=0.2.4 ; platform_system == "Windows"

# --- Automation ---
pyautogui>=0.9.54
pyperclip>=1.8.2
comtypes>=1.1.14 ; platform_system == "Windows"

# --- Browser Control (CDP) ---
websocket-client>=1.6.0
websockets>=12.0

# --- System ---
keyboard>=0.13.5
colorlog>=6.9.0
coloredlogs>=15.0
