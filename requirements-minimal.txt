# MINIMAL requirements - Gets CLI mode working immediately
# Use this if full requirements.txt fails
# Then you can run: python omni.py --test and python omni.py --cli "help"

loguru>=0.7.0
numpy>=1.26.0
psutil>=5.9.0
packaging>=24.0
websockets>=12.0
websocket-client>=1.6.0
